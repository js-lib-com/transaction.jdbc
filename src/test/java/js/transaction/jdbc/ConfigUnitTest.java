package js.transaction.jdbc;

import junit.framework.TestCase;

public class ConfigUnitTest extends TestCase {
	public void testJdbcAdapterConfig() throws Exception {
		// AppFactory appFactory = getAppFactory("jdbc-jsconfig.xml");
		//
		// TransactionContext database = appFactory.getInstance(TransactionContext.class);
		// assertNotNull(database);
		// assertEquals(database.getClass(), Classes.forName("js.db.JdbcAdapter"));
		//
		// DataSource dataSource = Classes.getFieldValue(database, "dataSource");
		// assertNotNull(dataSource);
	}
}
